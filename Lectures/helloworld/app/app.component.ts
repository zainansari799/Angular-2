import { Component } from '@angular/core';

@Component({
    moduleId: module.id, //define the relative path
    selector: 'root-app',
    templateUrl: 'app.component.html'
})
export class AppComponent {
}